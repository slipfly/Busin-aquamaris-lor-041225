doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title figma-frames-downloader
        link(rel="stylesheet", href="/css/main.css")
    body 
        form(method="post", action="/main") 
            .wrap 
                label(for="figmaFile") Вставьте ссылку на файл в фигме: 
                input(id="figmaFile", type="text", name='figmaFile') 
            .wrap 
                label(for="imageSize") Выберите качество выгружаемых фреймов: 
                select(id="imageSize", name="imageSize", required) 
                    option(value="0.5") x0.5 
                    option(value="1.0") x1.0 
                    option(value="2.0") x2.0 
            .wrap 
                label(for="imageFormat") Выберите формат выгружаемых фреймов: 
                select(id="imageFormat", name="imageFormat", required)
                    option(value="jpg") jpg 
                    option(value="png") png     
            .wrap 
                input(type="submit", value="Send")
